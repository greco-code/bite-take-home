# Local Express port.
PORT=4000

# Required Neon pooled connection string.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Comma-separated browser origins allowed by the API.
WEB_ORIGINS=http://localhost:3000
